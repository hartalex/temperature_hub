import temp_list from 'server/api/routes/temperatures/temp_list.js'
describe("Temp List Route", async () => {
  // it("temp_list failure", async() => {
  //   const req = {}
  //   const res = {status: jest.fn(),
  //   json: jest.fn()}
  //   const dbMock = jest.doMock('../../db/mongodb', ()=>{
  //   return jest.fn(() => {return {
  //   queryData: (dbobj, query, collection, callback)=>{
  //       callback("error")
  //   }}
  //   })
  // })
  //   const done = {}
  //   const expectedResult = {}
  //   const result = await temp_list(req, res, done)
  //   expect(res.status).toHaveBeenCalledWith(500)
  //   expect(res.json).toHaveBeenCalledWith({result:'fail', reason:'error'})
  //   //expect(res.json.mock.calls[0][0].result).toEqual('fail') // first call, first argument
  // })
  it("", async () => {})
})
